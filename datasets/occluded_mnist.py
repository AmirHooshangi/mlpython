"""
Module ``datasets.occluded_mnist`` gives access to the occluded MNIST dataset.

This is a multi-label classification dataset, where the task is to
reconstruct the original binary image of an MNIST digit from an occluded
version of it. The occlusion was performed by zeroing out a random patch
in the original image.

The original dataset from http://yann.lecun.com/exdb/mnist/ has been
preprocessed so that the inputs are binary. Occluded MNIST was
generated by Volodymyr Mnih.


| **References:**
| The MNIST database of handwritten digits
| LeCun and Cortes
| http://yann.lecun.com/exdb/mnist/

"""

import mlpython.misc.io as mlio
import numpy as np
import os
from gzip import GzipFile as gfile

def load(dir_path,load_to_memory=False):
    """
    Loads the occluded MNIST dataset.

    The data is given by a dictionary mapping from strings
    ``'train'``, ``'valid'`` and ``'test'`` to the associated pair of data and metadata.
    
    The inputs and targets have been converted to a binary format.

    **Defined metadata:**

    * ``'input_size'``
    * ``'target_size'``
    * ``'length'``

    """
    
    input_size=784
    target_size=784
    dir_path = os.path.expanduser(dir_path)

    def load_line(line):
        tokens = line.split()
        return (np.array([int(i) for i in tokens[:input_size]]), np.array([int(i) for i in tokens[input_size:]]))

    train_file,valid_file,test_file = [os.path.join(dir_path, 'occluded_mnist_' + ds + '.txt') for ds in ['train','valid','test']]
    # Get data
    train,valid,test = [mlio.load_from_file(f,load_line) for f in [train_file,valid_file,test_file]]

    lengths = [50000,10000,10000]
    if load_to_memory:
        train,valid,test = [mlio.MemoryDataset(d,[(input_size,),(target_size,)],[np.float64,np.float64],l) for d,l in zip([train,valid,test],lengths)]
        
    # Get metadata
    train_meta,valid_meta,test_meta = [{'input_size':input_size,'target_size':target_size,
                                        'length':l} for l in lengths]
    
    return {'train':(train,train_meta),'valid':(valid,valid_meta),'test':(test,test_meta)}


def obtain(dir_path):
    """
    Downloads the dataset to ``dir_path``.
    """

    dir_path = os.path.expanduser(dir_path)
    print 'Downloading the dataset'
    import urllib
    urllib.urlretrieve('http://www.cs.toronto.edu/~larocheh/public/datasets/occluded_mnist/mnist_occluded_u.mat',os.path.join(dir_path,'mnist_occluded_u.mat'))
    urllib.urlretrieve('http://www.cs.toronto.edu/~larocheh/public/datasets/occluded_mnist/mnist_occluded_v.mat',os.path.join(dir_path,'mnist_occluded_v.mat'))
    urllib.urlretrieve('http://www.cs.toronto.edu/~larocheh/public/datasets/occluded_mnist/mnist_occluded_valid_u.mat',os.path.join(dir_path,'mnist_occluded_valid_u.mat'))
    urllib.urlretrieve('http://www.cs.toronto.edu/~larocheh/public/datasets/occluded_mnist/mnist_occluded_valid_v.mat',os.path.join(dir_path,'mnist_occluded_valid_v.mat'))
    urllib.urlretrieve('http://www.cs.toronto.edu/~larocheh/public/datasets/occluded_mnist/mnist_occluded_test_u.mat',os.path.join(dir_path,'mnist_occluded_test_u.mat'))
    urllib.urlretrieve('http://www.cs.toronto.edu/~larocheh/public/datasets/occluded_mnist/mnist_occluded_test_v.mat',os.path.join(dir_path,'mnist_occluded_test_v.mat'))

    # Writing everything into text files, to allow for not loading the data into memory
    def write_to_txt_file(u,v,filename):
        f = open(filename,'w')
        for u_t,v_t in zip(u,v):
            for i in range(len(u_t)):
                f.write(str(int(u_t[i]>127))+' ')
            for i in range(len(v_t)-1):
                f.write(str(int(v_t[i]>127))+' ')
            f.write(str(int(v_t[-1]>127))+'\n')
        f.close()

    import scipy.io
    u = scipy.io.loadmat(os.path.join(dir_path,'mnist_occluded_u.mat'))['dat']
    v = scipy.io.loadmat(os.path.join(dir_path,'mnist_occluded_v.mat'))['dat']
    write_to_txt_file(u,v,os.path.join(dir_path,'occluded_mnist_train.txt'))

    u = scipy.io.loadmat(os.path.join(dir_path,'mnist_occluded_valid_u.mat'))['dat']
    v = scipy.io.loadmat(os.path.join(dir_path,'mnist_occluded_valid_v.mat'))['dat']
    write_to_txt_file(u,v,os.path.join(dir_path,'occluded_mnist_valid.txt'))

    u = scipy.io.loadmat(os.path.join(dir_path,'mnist_occluded_test_u.mat'))['dat']
    v = scipy.io.loadmat(os.path.join(dir_path,'mnist_occluded_test_v.mat'))['dat']
    write_to_txt_file(u,v,os.path.join(dir_path,'occluded_mnist_test.txt'))

    print 'Done                     '
